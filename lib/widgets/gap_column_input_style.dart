import 'package:flutter/material.dart';

class GapCInput extends StatelessWidget {
  // final double width;
  // final double height;
  // final Widget child;

  const GapCInput({
    Key? key,
    // required this.width,
    // required this.height,
    // required this.child,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SizedBox(height: 16);
  }
}
